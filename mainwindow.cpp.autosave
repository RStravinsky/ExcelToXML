#include "mainwindow.h"
#include "ui_mainwindow.h"

MainWindow::MainWindow(QWidget *parent) :
    QMainWindow(parent),
    ui(new Ui::MainWindow)
{
    ui->setupUi(this);
}

MainWindow::~MainWindow()
{
    delete ui;
}

void MainWindow::on_excelBtn_released()
{
    QString fileName = QFileDialog::getOpenFileName(this,tr("Wybierz plik"), "/home/jana", tr("Excel (*.xls *.xlsx)"));
    qDebug() << "Excel file path: " << fileName << endl;
}
